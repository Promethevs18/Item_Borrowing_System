<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ItemBorrowingSystem" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/backgroundWhenLight</item>
        <item name="android:textColor">@color/initialColorWhenLight</item>
    </style>

    <style name="Theme.ItemBorrowingSystem" parent="Base.Theme.ItemBorrowingSystem" />
</resources>